#!/bin/bash

#This script delete converter code logs each x seconds
#This script convert the Raw files of the QHYCCD into FITS format, once they are with the FITS format, they are sent into the directory where the nas look

while :; do
sleep 90
    echo "" > /tmp/logConverter.txt
    echo "clear"
done
